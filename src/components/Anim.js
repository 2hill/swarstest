import React from 'react';
import PropTypes from 'prop-types';

const Anim = (props) => (

    <div className="loader">

    </div>
);

Anim.propTypes = {
    message: PropTypes.string
};

export default Anim;
